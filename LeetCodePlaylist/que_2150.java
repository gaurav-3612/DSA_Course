package LeetCodePlaylist;

public class que_2150 {
    // use hashmap to store key ans arr[i] and value as frequescy
    // then add those keys in answer whose keys freq is one and key+1,key-1 do not exist in hashmap
}
