package LeetCodePlaylist;

public class que_532 {
    // used hashmap to store values and their frequencies
    // used the concept that if k>0 and hashMap contains value+k then it means difference exists
}
