package LeetCodePlaylist;

public class question_48 {
    // make two functions
    // 1-- transpose of matrix (reverse(i,j) with (j,i))
    // 2-- reverse every row (two pointers left and right swap them until left<right)
}
